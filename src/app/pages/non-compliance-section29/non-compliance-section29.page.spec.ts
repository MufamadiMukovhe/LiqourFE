import { ComponentFixture, TestBed } from '@angular/core/testing';
import { NonComplianceSection29Page } from './non-compliance-section29.page';

describe('NonComplianceSection29Page', () => {
  let component: NonComplianceSection29Page;
  let fixture: ComponentFixture<NonComplianceSection29Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(NonComplianceSection29Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
