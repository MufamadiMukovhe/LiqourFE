import { ComponentFixture, TestBed } from '@angular/core/testing';
import { BoardConsiderationORHearingPage } from './board-consideration-orhearing.page';

describe('BoardConsiderationORHearingPage', () => {
  let component: BoardConsiderationORHearingPage;
  let fixture: ComponentFixture<BoardConsiderationORHearingPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(BoardConsiderationORHearingPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
