import { ComponentFixture, TestBed } from '@angular/core/testing';
import { NonComplianceSection55Page } from './non-compliance-section55.page';

describe('NonComplianceSection55Page', () => {
  let component: NonComplianceSection55Page;
  let fixture: ComponentFixture<NonComplianceSection55Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(NonComplianceSection55Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
